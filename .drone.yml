pipeline:
  test:
    image: node:5
    commands:
      - npm install --quiet --color > npm-install.log
      - npm version
  debug:
    image: dryclean/drone-debug
    commands:
      - echo -e $PLUGIN_GIT_ID
      - echo -e $PLUGIN_GIT_PRIVATE
  install-cat:
    image: busybox
    commands:
      - cat npm-install.log

